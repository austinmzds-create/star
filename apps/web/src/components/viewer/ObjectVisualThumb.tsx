'use client';

/**
 * 跨域契约导出（冻结）：<ObjectVisualThumb uid={string} /> —— 卡内预览
 * 缩略块，内部自查目录/自生成缩略图/点击 openObjectViewer；对任何 uid
 * 都能渲染出内容。实现见 ViewerPreviewBlock（同一组件，双名导出）。
 */
export { ViewerPreviewBlock as ObjectVisualThumb } from './ViewerPreviewBlock';
