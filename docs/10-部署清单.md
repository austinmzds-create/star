# 部署清单(本轮全部改动)

> 分支:`claude/influencer-quality-assessment-6rd4lv`(PR #1) · HEAD:`8043b3c`
> 覆盖:P0/P1 修复、前端加载提速、图片/缩略图缓存、**OSS 签名直传(支持私有 bucket)**、素材优化、review 修正。
> 原则:**先部署代码 → 冒烟验证 → 再切 OSS 私有**,全程零停机。

---

## 0. 前置确认(1 分钟)

- [ ] 后端如何跑?(docker-compose / 宝塔 / pm2 / systemd)—— 决定第 2 步用哪种重启方式。
- [ ] 数据库是 PostgreSQL 吗?
      `python -c "from app.config import settings; print(settings.database_url)"`
      不是 `postgresql+psycopg://...` 就先切 Postgres(SQLite 重建丢数据 + 并发写锁)。
- [ ] `server/.env` 里 OSS 的 `OSS_ACCESS_KEY_ID/SECRET` 已配(签名直传依赖它,现有已配)。

---

## 1. 拉取代码

```bash
cd <项目目录>
git fetch origin
git checkout claude/influencer-quality-assessment-6rd4lv
git pull --ff-only origin claude/influencer-quality-assessment-6rd4lv
# 确认 HEAD 是 8043b3c
git rev-parse --short HEAD
```

## 2. 后端部署

无需手动迁移:启动时 `ensure_columns()` 会自动补新列(`materials.is_public`、`promotions.fail_proof_oss_keys`)。
按你的运行方式二选一:

```bash
# A) docker-compose
docker compose build api && docker compose up -d api
# B) 宝塔/systemd/pm2:先装依赖再重启(依赖无新增,可跳过 pip)
pip install -r server/requirements.txt
# 重启你的 api 进程(systemctl restart <svc> / pm2 restart <app> / 宝塔重启)
```

- [ ] 健康检查:`curl -s https://talent.jisheng.yun/api/health` → `{"ok":true}`
- [ ] 启动日志无 `[启动告警] 生产环境正在使用 SQLite`(有则回到第 0 步切 Postgres)。

## 3. 前端部署

```bash
cd web
npm ci        # 或 npm install
npm run build # 产出 web/dist
# 把 web/dist/* 发到 nginx 站点根目录(替换旧文件)
```

- [ ] 发布后强刷一次页面(Ctrl/Cmd+Shift+R),确认能正常登录进入。

---

## 4. 冒烟验证(务必逐项过一遍,此刻 bucket 仍是公共读写)

**上传(签名直传已自动生效)**
- [ ] 产品中心 → 编辑商品图,传一张图 → 保存 → 列表/详情正常显示。
- [ ] 素材 → AI视频 → 一次多选 2~3 个视频 → 每个自动生成一条(标题=文件名,无需文案)。
- [ ] 素材 → 质检报告 → 传一张**图片**(不是 PDF)→ 能作为图片预览。
- [ ] 素材 → 达人成片(仅管理员可见该 tab)→ 传一条 → 显示「未公开」→ 点「公开」→ 变「已公开」。
- [ ] 传一个 >5MB 的视频(走分片直传)→ 成功、可播放。

**权限 / 可见性**
- [ ] 用**商务**账号:产品里**看不到**「达人成片」tab;素材计数不含成片。
- [ ] 达人端(H5)打开该产品资料:**未公开的成片不出现**;把成片「公开」后再刷新 → 出现。

**其它高频路径**(抽查)
- [ ] 达人:管理员用带空格手机号建档,达人用标准 11 位登录 → 进同一档案(不建重复空号)。
- [ ] 寄样审批/视频审批/删除等操作:失败会弹提示(不再静默)。

> 任一项不通过就先别做第 5 步,把现象发我。

---

## 5. OSS 切私有(把公共写关掉,消除对外敞口)

**确认第 4 步上传都 OK 后再做。** CORS 无需改(已含 `ExposeHeader: ETag`)。

1. [ ] 阿里云 OSS 控制台 → Bucket `viceo-public` → **权限管理 → 读写权限 → 改为「私有」→ 确定**。
2. [ ] 再验证一次:传图 / 传视频 / 看已有图能否显示(预览走后端 `/api/files` 代理,用 AK 读,不受影响)。

> 原理:配了 AK 后前端走**签名 URL**直传,公有/私有都能用;所以先部署签名版、后切私有,不会断。

---

## 6. 运维加固(可与上线同批或稍后)

**6.1 补索引**(数据量涨起来前执行一次;`create_all` 不会给已存在表补索引)
```sql
CREATE INDEX IF NOT EXISTS ix_sample_orders_status   ON sample_orders(status);
CREATE INDEX IF NOT EXISTS ix_sample_orders_created  ON sample_orders(created_at);
CREATE INDEX IF NOT EXISTS ix_video_tasks_status     ON video_tasks(status);
CREATE INDEX IF NOT EXISTS ix_video_tasks_created    ON video_tasks(created_at);
CREATE INDEX IF NOT EXISTS ix_promotions_auth_status ON promotions(auth_status);
CREATE INDEX IF NOT EXISTS ix_follow_up_tasks_status ON follow_up_tasks(status);
CREATE INDEX IF NOT EXISTS ix_influencers_updated    ON influencers(updated_at);
CREATE INDEX IF NOT EXISTS ix_products_updated       ON products(updated_at);
```

**6.2 nginx**(让"刷新"真正命中缓存 + 与上传上限一致)
```nginx
location = /index.html { add_header Cache-Control "no-cache"; }
location /assets/     { add_header Cache-Control "public, max-age=31536000, immutable"; }
client_max_body_size 200m;
```
验证:`curl -sI https://talent.jisheng.yun/ | grep -i cache-control` 应含 `no-cache`。

**6.3 AccessKey 轮换(安全,建议做)**
之前的 AK 在对话里明文传过。RAM 新建一对 AK → 更新 `server/.env` 的 `OSS_ACCESS_KEY_ID/SECRET` → 重启 api → 删除旧 AK。

**6.4 OSS 生命周期规则**
给 bucket 配「删除未完成的分片上传(碎片)」规则(如 3 天),回收分片失败残留。

**6.5(可选)多 worker**
Docker 已默认 2 个 worker;需要更多设 `UVICORN_WORKERS=<CPU核数>`(须 Postgres)。

---

## 7. 历史数据说明(非阻塞)

- 旧的 0 字节商品图/素材无法由代码恢复,显示「图片失效」占位 → 对应产品/素材**重新上传一次**即可。
- 存量达人成片(迁移后 `is_public` 为 NULL)默认视为**未公开**,不对达人展示;需要公开的逐条点「公开」。

---

## 8. 回滚

代码问题:`git checkout main`(或上一个已上线 tag)→ 重新 `npm run build` + 重启 api。
`ensure_columns` 只加列不删列,回滚代码不影响已建列(旧代码忽略新列即可),数据安全。
若已切 OSS 私有又需回退旧的公共直传前端:把 bucket 改回「公共读写」即可(签名版在公共 bucket 也能用,通常无需回退)。
